{
  "name": "openclaw-slack-router",
  "version": "1.0.0",
  "description": "Slack event router for OpenClaw multi-instance platform",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test src/**/*.test.js",
    "lint": "eslint src/",
    "zip": "cd src && zip -r ../dist/slack-router.zip ."
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
